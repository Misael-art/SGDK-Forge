interface:
  display_name: "Forward Kinematics Rigging"
  short_description: "Cadeias de juntas procedurais em fix16 com budget real de scanline"
  brand_color: "#3F6B55"
  default_prompt: "Use $forward-kinematics-rigging para estruturar rigs procedurais (FK) no Mega Drive: joint schema, ordem topologica, LUT/trig em fix16 e plano de prova em ROM."

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
