# Failure Patterns

Registre aqui falhas, falsos positivos, tentativas ruins e decisoes que nao devem ser repetidas sem nova evidencia.

| Data | Classificacao | Contexto | Falha observada | Causa provavel | Mitigacao | Evidencia |
|---|---|---|---|---|---|---|
| [DATA] | `local_note` | [cena/sistema] | [o que falhou] | [causa] | [como evitar] | [log/screenshot/hash] |
| 2026-08-06 | `local_note` | tabela que virou PESSIMIZACAO | Troquei `F16_cos/F16_sin` da cadeia do boss por uma tabela de 64 entradas esperando o mesmo ganho que a tabela deu na agua. Resultado: **cpu p99 92% -> 107%, frames estourados 3 -> 225**. Revertido | Meu indexador fazia `deg % 360` e `deg * 64 / 360` — **duas divisoes de 32 bits por segmento**, e o 68000 nao tem divisao rapida. O `F16_cos` do SGDK ja e tabela com indexacao barata | **Tabela nao e automaticamente mais rapida que funcao.** Ela ganhou em `raster.c` porque o indice era MASCARA (`& 63`); perdeu aqui porque exigiu divisao. O que importa e o custo do INDICE, nao a existencia da tabela | `r5_lever2` (92%) vs `r5_table` (107%) vs `r5_revert` (92%) |
| 2026-08-06 | `local_note` | gastei degradacao antes de procurar desperdicio | Com o holofote a arena foi a 106%. Gastei imediatamente a **alavanca 2** do §5.1 (7 -> 5 segmentos do galho) sem antes procurar desperdicio — violando a licao que eu mesmo tinha registrado na sessao da arena | Reflexo de pegar a ferramenta documentada em vez de investigar. A alavanca funcionou (106% -> 92%), o que mascarou o erro de ordem | **Ordem correta: desperdicio -> otimizacao sem perda -> degradacao.** E quando a degradacao for inevitavel, deve pagar quem CAUSOU o custo: aqui o holofote (feature nova) foi reduzido de 5 para 3 sprites e resolveu os 3 frames restantes, em vez de cortar mais o boss | `r5_light` 106% -> `r5_lever2` 92% -> `r5_final` 82% |
| 2026-08-06 | `local_note` | screenshot como prova de performance | Olhei a captura do lago, vi a distorcao e a paleta submersa funcionando, e ia declarar R3/R4 prontos. Os gates diziam outra coisa: **cpu p99 111% e 306 frames acima do budget** — a cena nao cabia num frame | `F16_sin()` chamado uma vez POR LINHA POR FRAME para ~74 linhas. Um efeito pode estar visualmente perfeito e ainda assim nao caber no orcamento; screenshot nao mede tempo | **Rodar os gates ANTES de declarar um efeito pronto, sempre.** Corrigido com tabela de 64 entradas pre-computada: mesma imagem, **111% -> 71%, 306 -> 0 frames**. Trigonometria por frame em MD e quase sempre tabela, nao funcao | `lake_fix` (FAIL) vs `lake_opt` (PASS) |
| 2026-08-06 | `local_note` | gate lendo duas coisas na mesma amostra | `parallax_layer_speeds` reprovou o terreno no lago: media `hs_terrain=2` com `camera_x=0` | O probe amostrava `s_hscrollA[200]`, que agora carrega o scroll de parallax **mais** a ondulacao da agua. Dois efeitos somados numa amostra so, e o gate so conhece a formula de um deles | Telemetria deve publicar o valor da grandeza que o gate mede, nao o valor final da porta. Passei a publicar `-cameraX` (o parallax puro); a ondulacao e outro assunto e precisa do proprio gate se um dia importar | `lake_fix` vs `lake_opt` |
| 2026-08-06 | `local_note` | otimizacao que quebrou efeito animado | O efeito R3 (distorcao senoidal da agua) nao aparecia: a tabela de HScroll nunca era reconstruida | A otimizacao "pular rebuild quando a camera nao move", que eu havia adicionado para salvar a arena do boss (96%->78% de CPU), tambem pula a reconstrucao quando a distorcao e ANIMADA. Camera parada nao significa tabela estatica | Otimizacao baseada em "entrada nao mudou" precisa considerar TODAS as entradas, nao so a obvia. A condicao virou `(cameraX == s_lastCameraX) && (s_waterline < 0)`. Toda otimizacao de cache deve listar o que a invalida | `lake_w4` (sem efeito) vs `lake_fix` (com efeito) |
| 2026-08-06 | `local_note` | derivar scanline de um contador saturado | O efeito R4 (troca de paleta submersa) nunca disparava | Eu derivava a scanline de `stop * LINES_PER_STOP`, mas `stop` satura em `RASTER_SKY_STOPS` (12), logo a linha calculada travava em 144 — e a linha d'agua estava em 150. O H-int continua disparando o frame inteiro, mas o contador do gradiente nao | Nao reutilizar um contador que SATURA como se fosse um relogio. Um contador de linha proprio (`s_hintLine`) que so incrementa resolve | CRAM dump: PAL1[1..4] inalterada antes, azul/ciano depois |
| 2026-08-06 | `needs_human_review` | gradiente de ceu apos as mudancas de agua | A primeira parada do gradiente passou a durar mais scanlines, em AMBAS as cenas (lago e fase normal). Visualmente o topo virou uma faixa amarela grande | **NAO DETERMINADO.** O H-int esta correto na leitura, `s_skyStop` reseta em `RASTER_frameStart`, e todos os gates passam (parallax exato, cpu p99 59%) | **Nao tentar consertar por comparacao de screenshot** — foi exatamente o erro que custou uma sessao inteira na medicao de parallax. A correcao e exportar `s_skyStop` e `s_hintLine` no bloco KRB1 e LER, como foi feito para o parallax. Registrado como defeito aberto no proprio codigo | `stage_regress` vs `stage_after_water` |
| 2026-08-06 | `needs_human_review` | ambiente / flatpak bloqueado | Build e capturas passaram a falhar com `fallocate: Nao ha espaco disponivel no dispositivo`, em 0.07 s, antes de compilar. O host tinha 46 GB livres, o que mascarou o problema por varias tentativas | **`/run/user/1000` (XDG_RUNTIME_DIR, tmpfs de 1,5 GB) estava 100% cheio.** O flatpak precisa de espaco la para montar o sandbox, entao `flatpak run` falhava na largada. Os 1,5 GB inteiros eram `codex-desktop/tmp/pytest-of-misael` — tres `Metroid.state` de 512 MB de testes de OUTRO app | **Quando flatpak falhar com ENOSPC, checar `/run/user/$UID`, nao so `df` do projeto.** Provavelmente e a mesma causa dos `window_timeout` intermitentes do BlastEm, que eu havia atribuido a processos orfaos — aquele diagnostico estava incompleto. Nao deletei: dados de outro app | `/tmp/build_ability2.log`, `df -h /run/user/1000` |
| 2026-08-06 | `local_note` | edicao por script, 4a ocorrencia | `PLAYTEST_STATE_ABILITY_USED` nao foi inserido no header: o `replace` nao casou por diferenca de ESPACAMENTO no comentario alinhado | Quarta vez na mesma sessao. Ancorei numa string com espacamento que eu supus, sem conferir o arquivo | Para insercao em header com comentarios alinhados, usar a ferramenta de edicao que **falha alto** quando o alvo nao casa, em vez de `str.replace` que falha em silencio. E sempre `grep` no simbolo novo antes de buildar | `scene_stage.c:263` undeclared |
| 2026-08-06 | `local_note` | gate com deteccao de contexto por bit compartilhado | `playtest_boss_combat` reprovou uma captura de FASE exigindo `boss_hurt`/`boss_dead` | Eu detectava "captura de boss" por QUALQUER bit de combate, mas `kirby_hurt` passou a ser alcancavel na fase quando adicionei dano de inimigo. O bit deixou de ser exclusivo do boss | Deteccao de contexto tem de usar bits EXCLUSIVOS daquele contexto (`PLAYTEST_BOSS_ONLY = boss_hurt \| boss_dead`). Bit compartilhado como discriminador quebra assim que outra cena passa a produzi-lo | `ability2` FAIL -> PASS apos a correcao |
| 2026-08-06 | `local_note` | texto sob Shadow/Highlight global | "PRESS START" no titulo custou TRES capturas: (1) no plano WINDOW nao apareceu, (2) em BG_A com `VDP_setTextPriority(TRUE)` saiu como bloco cinza solido em vez de glifos, (3) so funcionou com S/H DESLIGADO | (1) o WINDOW nao tem tamanho ate `VDP_setWindowVPos` ser chamado; (2) a fonte do SGDK nao coexiste com S/H global sem tratamento especial | **Cena sem efeito que dependa de S/H deve rodar com S/H desligado.** `doc/PALETTES.md` 2.2 trava S/H para cenas de GAMEPLAY por causa do vortex de inalar; titulo e game over nao tem essa necessidade. Brigar com a fonte custou mais que aceitar isso | `title1`, `title2`, `title3` |
| 2026-08-06 | `local_note` | prompt piscante vs captura estatica | O prompt piscava (34 de 60 frames ligado) e duas capturas o pegaram no frame apagado — indistinguivel de "nao renderiza" | Screenshot estatico nao consegue provar animacao; so consegue pega-la desligada | **Affordance essencial nao pisca.** Se um elemento precisa ser provado por captura, ele tem de estar sempre visivel. Animacao e para o jogador, nao para o gate | `title3` (ausente) vs `title_final` (visivel) |
| 2026-08-06 | `local_note` | ordem de declaracao em C, 3a ocorrencia | Inseri `SKY_NIGHT` e `s_useNight` DEPOIS do H-int que os usa; build quebrou com `undeclared` | Terceira vez nesta sessao que uma insercao por script cai depois do primeiro uso | Ao inserir simbolo novo em C por script, ancorar na DEFINICAO relacionada (aqui, logo apos `SKY_GRADIENT`), nunca numa funcao arbitraria | `raster.c:71` |
| 2026-08-06 | `local_note` | analise de audio / formato do dump | Li `audio.raw` do BlastEm como int16 e conclui que a musica era uma "onda quadrada em escala cheia" com pico de -0.1 dBFS e RMS de -5.7 dBFS. **Tudo errado.** O arquivo e **float32, 48 kHz, estereo** — o pico real era -18.5 dBFS, com folga saudavel | Assumi o formato em vez de conferir. O proprio `blastem.log` do bundle dizia, em texto claro: `Initialized audio at frequency 48000 with a 1024 sample buffer, 32-bit float format` | **Conferir o formato no log antes de interpretar qualquer dump binario.** A informacao estava a um `grep` de distancia. Pior: eu MUDEI CODIGO (silenciei PSG, subi TL) com base na leitura errada, e quase reivindiquei ter consertado um problema que nunca existiu | `audio_proof2` lido como int16 vs float32 |
| 2026-08-06 | `local_note` | teste de controle que salvou o diagnostico | Desligar a musica para isolar a fonte do sinal devolveu RMS MAIOR sem musica (27550) do que com (16906) — resultado impossivel que expos a leitura errada do formato | O teste de controle nao "funcionou" no sentido de confirmar a hipotese; ele produziu um absurdo, e o absurdo foi o sinal util | Quando um teste de controle devolve algo fisicamente impossivel, **o instrumento esta errado, nao o sistema**. Parar e checar o instrumento antes de teorizar sobre o sistema | `audio_silent` vs `audio_proof2` |
| 2026-08-06 | `local_note` | script de playtest que se auto-impede | O script do boss segurava B do inicio ao fim. Resultado: `kirby_hurt` ficou NAO, porque Kirby engolia toda maca antes de ser atingido — o caminho de dano NELE nunca era exercitado | Um script otimo em jogar e pessimo em cobrir. Jogar bem evita justamente os estados que precisam ser provados | Script de cobertura precisa **tomar dano de proposito**. Inseri 300 frames parado sem inalar antes de contra-atacar. `kirby_hurt` passou a SIM | `bosstest1` (NAO) vs `bosstest_final` (3/3) |
| 2026-08-06 | `local_note` | duracao de script vs janela de captura | `playtest_completed` reprovou: script de ~2400 frames (40 s) sob captura de 32 s parou no passo 6 de 7 | Escrevi o script sem somar a duracao contra o `--warmup-seconds` da captura | Somar os frames do script e garantir folga. Encurtei para ~1610 frames (~27 s) sob captura de 30 s. O gate `playtest_completed` existe exatamente para pegar isso | `bosstest4` (step 6, finished=0) |
| 2026-08-06 | `local_note` | gate exigindo o que a cena nao promete | `playtest_coverage` reprovou uma captura de boss por faltar 8 dos 11 estados de locomocao | O gate assumia que TODA captura de playtest tenta cobrir locomocao. O script do boss cobre o loop de combate; locomocao e trabalho do script de fase | Gate precisa saber o CONTEXTO. Detecta captura de boss pelos bits de combate e reporta locomocao como informativo. Um gate que reprova a cena por nao fazer algo que ela nunca prometeu treina o time a ignorar gates | `bosstest4` (FAIL) vs `bosstest_final` (soft PASS) |
| 2026-08-06 | `local_note` | adicionar cena nova | Adicionei `APP_SCENE_STAGE_PLAYTEST = 5` ao enum e liguei o dispatch, mas a captura com `--target-scene 5` teria caido silenciosamente na cena default | `PROBE_resolveBootScene` valida `sceneId < sceneCount`, e `sceneCount` vem de `APP_SCENE_COUNT`, um `#define` em `src/core/app.c` — arquivo DIFERENTE do enum em `inc/game_vars.h`. Esquecer de subir a constante nao gera erro de compilacao, so um fallback mudo | Ao adicionar cena: enum em `inc/game_vars.h` + `APP_SCENE_COUNT` em `src/core/app.c` + nome em `APP_sceneName` + `case` no enter e no update. Sao 4 lugares. O gate `scene_identity` pega o fallback, entao SEMPRE conferir esse gate na primeira captura de uma cena nova | `APP_SCENE_COUNT` 5 -> 6 -> 7 nesta sessao |
| 2026-08-06 | `local_note` | modelo do gate de cor de screenshot | `screenshot_color_count` reprovou com 262 cores contra teto de 174 numa cena correta. O teto vinha de "entradas de CRAM uteis x 3" (normal/sombra/highlight do S/H) | **O modelo quebra com raster.** O gradiente de ceu percorre UMA entrada de CRAM por 12 stops dentro do mesmo frame, entao essa unica entrada rende ate 36 cores de tela sozinha. O teto nao e derivavel sem conhecer os stops de cada cena | Gate rebaixado para SOFT, com o motivo escrito nele. A restricao REAL de hardware e ocupacao de CRAM, que `color_budget` ja cobra contra o limite verdadeiro de 58. **Enfraquecer um gate tem de ser dito em voz alta**: este media a grandeza errada em cena com raster | `boss_arena5/*/gate_report.json` |
| 2026-08-06 | `local_note` | ordem de publicacao na telemetria | `sh_enabled` chegou como 0 na arena mesmo com `VDP_setHilightShadow(TRUE)` chamado | `PROBE_STAGE_reset()` zera todos os campos publicados e rodava DEPOIS do publish do S/H, apagando-o silenciosamente | Publicar sempre DEPOIS do reset. Melhor ainda: o gate pegou isso, o que confirma que vale ter telemetria de INTENCAO mesmo sem poder ler o hardware | `boss_arena5` (0) vs `boss_arena_final` (1) |
| 2026-08-06 | `local_note` | processos de captura acumulando | Cinco capturas seguidas falharam com `window_timeout`. `flatpak ps` mostrou vazio, `flatpak kill com.retrodev.blastem` nao matou nada, e ainda assim `ps -eo pid,comm` listava varios `blastem.bin` vivos bloqueando a deteccao de janela | Os processos sobrevivem ao `flatpak kill` e nao aparecem de forma confiavel em `flatpak ps`. Eles se acumulam a cada captura falha, piorando o problema | Antes de uma serie de capturas, limpar por PID: `for p in $(ps -eo pid,comm \| awk '$2 ~ /^blastem/ {print $1}'); do kill -9 $p; done`. Nunca `pkill -f blastem` — casa com o proprio comando do agente | `boss_arena2` a `boss_arena4` falharam; apos a limpeza, `boss_arena5` passou |
| 2026-08-06 | `local_note` | versionamento do bloco de telemetria | Rodar `gates.py` num bundle antigo (KRB1 de 20 words) apos a ROM passar a emitir 23 words derrubou o gate inteiro com `KeyError: 'playtest_step'` | O leitor assumia que todo campo declarado existe. Bloco de telemetria e um FORMATO VERSIONADO: bundles antigos ficam no disco e continuam sendo lidos | Todo campo novo no bloco precisa de leitura tolerante (`if campo not in block`) que degrade para SKIP com o motivo. **Gate que crasha e pior que gate que avisa**: uma excecao torna o relatorio inteiro ilegivel em vez de deixar um gate inconclusivo | reteste de `probe_p5` apos o KRB1 crescer |
| 2026-08-08 | `local_note` | hipotese minha, falsificada pela medicao | Portei do projeto irmao GROK BUILD o gate de solidez de indice (`center_idx0 < 5%`, `opaque > 35%`) e previ que ele explicaria por que P1..P4 foram reprovadas. **Nao explicou.** O Kirby do P1 passa nos 8 frames com `center_idx0=0.0%`, `opaque=50-60%` e **9 tons de corpo** — mais tons que o placeholder atual (5) e mais que o R6 do GROK (6), que foi aceito com nota 7.2 em personagem | Confundi duas classes de falha. O gate mede **corpo virou buraco de hardware** (indice 0 = transparencia do VDP), que foi o que matou a integracao de arte AI do GROK. O que matou o P1 foi **forma/apelo procedural**, que nao tem assinatura em histograma de indice. Minha hipotese secundaria (riqueza tonal como proxy de qualidade) tambem morreu no mesmo dado | **Manter o gate, mas nao vende-lo como o que nao e.** Ele e seguro barato contra um defeito real e invisivel em preview — achou 1 defeito legitimo no P3 (`frame_06_float`, `center_idx0=20.8%`). Nao substitui model sheet aprovado por humano. Esta e a razao de `technical_pass_visual_fail` ser um STATUS NOMEADO e nao um numero: a classe de falha que sobra e justamente a que nenhuma estatistica de pixel captura | `out/logs/solidity_p1_vs_archive.json`; auto-teste do gate em `tools/harness/sprite_solidity.py --self-test` |
| 2026-08-06 | `local_note` | backdrop sob Shadow/Highlight | Arena do boss renderizou inteira em roxo escuro. O backdrop apontava para o indice 0 de uma paleta cuja entrada 0 e a CHAVE DE TRANSPARENCIA magenta `(255,0,255)`; com S/H global e sem tile de prioridade cobrindo, o backdrop renderiza SOMBREADO, e magenta pela metade e exatamente aquele roxo | Reaproveitei a paleta de uma imagem cujo indice 0 e chave de transparencia, sem dar ao backdrop uma cor real | Em cena com S/H, **dar ao indice do backdrop uma cor de verdade** com `PAL_setColor`. Chave de transparencia nunca deve ser o backdrop | `boss_run1` (roxo) vs `boss_lever1` (azul) |
| 2026-08-06 | `local_note` | captura / `window_timeout` | Duas capturas seguidas falharam com `reason=window_timeout` e `blastem.log` de 0 bytes. Matei o processo `blastem.bin` orfao por PID e AINDA falhou | O bloqueio nao era o processo `blastem.bin` e sim uma **instancia flatpak presa** da execucao anterior, visivel em `flatpak ps` e nao em `ps -eo comm` | Ao ver `window_timeout`, checar **`flatpak ps`** alem de `ps`. A instancia costuma se liberar sozinha em ~1 min; a tentativa seguinte funcionou sem intervencao | `boss_run2`, `boss_run3` falharam, `boss_run4` passou |
| 2026-07-30 | `local_note` | probe / leitura de VRAM — **LICAO CORRIGIDA** | Auditoria de prioridade lia 0 entradas de BG_A e eu registrei a causa como "nao determinada", levantando suspeita de delay de leitura de VRAM ou restricao em display ativo. **ESSA LICAO ESTAVA ERRADA.** As leituras sempre funcionaram: diagnostico cru devolveu `0xA083` (tile 131, prioridade 1, paleta 1) para BG_A linha 22 | A causa real foi um `str.replace` meu que NAO APLICOU (a string alvo tinha mudado), deixando o passo de amostragem em 128 bytes = linhas 0-15, onde BG_A esta vazio. Eu acreditei que a edicao tinha aplicado e culpei o hardware | **Verificar que toda edicao por script APLICOU antes de interpretar o resultado.** `grep` no valor novo, nao confiar no print do proprio script. Segunda ocorrencia da mesma classe de erro nesta sessao (a primeira foi o bloco de telemetria caindo em `SCENE_stageEnter`) | diagnostico cru em `probe_vramdiag`, bloco KRB1 words 16-19 |
| 2026-07-30 | `local_note` | amostragem de telemetria | Primeira versao da auditoria de prioridade usava passo de 128 bytes (1 linha de nametable) e cobria so as linhas 0-15, perdendo o terreno nas linhas 22-29. O gate reportava "0 de 0 amostrados" e isso parecia aprovacao | Escolhi o passo sem conferir contra o layout real das camadas | Amostragem deve cobrir o INTERVALO ONDE O CONTEUDO ESTA, e o relatorio precisa mostrar o denominador. "0 de 0" e vacuo e tem de ser visivelmente diferente de "0 de 16" | comparar `probe_prio` e `probe_final` |
| 2026-07-30 | `local_note` | prioridade de tile vs area de texto | Com Shadow/Highlight global, `VDP_clearTextArea` em BG_A gerava 16 de 16 entradas amostradas em prioridade 0 | `VDP_clearTextArea` preenche a area com o glifo BRANCO da fonte, cujo indice de tile e NAO ZERO e cuja prioridade e 0. Sao invisiveis, mas continuam sendo tiles de fundo em prioridade 0 | Em cena com S/H ligado, usar `VDP_clearPlane` e NAO `VDP_clearTextArea`. Corrigido: BG_A saiu de 16/16 violacoes para 0 | `probe_prio` (16/16) vs `probe_prio_fix` (0) |
| 2026-07-30 | `needs_human_review` | raster / H-interrupt vs DMA | Entre 17 e 31 entradas CONTIGUAS de CRAM (PAL0[1..15] + PAL1 inteira) sobrescritas com um verde uniforme `0x04A0`, de forma NAO DETERMINISTICA (numero de entradas variava por execucao). PAL2/PAL3 intactas. Tela ficava verde | O H-int escreve `VDP_CTRL_PORT` (endereco de CRAM) e depois `VDP_DATA_PORT`. O SGDK faz flush da fila de DMA dentro de `SYS_doVBlankProcess`, e um DMA tambem e "escreve a porta de controle, depois transfere". Quando o H-int caia ENTRE esses dois passos, sobrescrevia o endereco pendente e o DMA da tabela de HScroll aterrissava no CRAM | **Nenhuma interrupcao pode cair no meio de uma sequencia de portas do VDP.** Mascarar o H-int durante TODO o VBlank via `SYS_setVIntCallback` e re-armar no inicio do frame. O H-int em si estava correto isolado; a vitima era o codigo interrompido. Vale para qualquer projeto que combine H-int com fila de DMA do SGDK | dumps de CRAM: `fase1_settled` 31 entradas, `fase1_noflash` 17, `fase1_hintfix` **0** |
| 2026-07-30 | `local_note` | diagnostico por eliminacao | Meu primeiro suspeito pela corrupcao de CRAM foi o flash de paleta da habilidade (`PAL_setColor`), porque era a unica escrita de paleta nova. Estava ERRADO: desliguei o flash e a corrupcao persistiu (17 entradas) | Escolhi o suspeito pela proximidade temporal da mudanca, nao por mecanismo. O flash escrevia 1 entrada; o defeito afetava 31. A aritmetica ja dizia que nao fechava | Quando o sintoma tem ESCALA muito maior que o suspeito (1 entrada vs 31), descartar o suspeito por aritmetica ANTES de gastar um build para testa-lo | `fase1_noflash` vs `fase1_settled` |
| 2026-07-30 | `local_note` | pan de abertura de cena | Na primeira versao o pan congelava `KIRBY_update` e `ENEMY_updateAll` inteiros. Kirby e os inimigos ficaram FLUTUANDO no ar durante todo o pan porque a gravidade nunca rodava | Confundi "jogador sem controle" com "mundo parado" | Durante cutscene/pan, suprimir apenas o INPUT (passar `held=0, pressed=0`), nunca a simulacao. Pan sobre mundo vivo le como intencional; sobre mundo congelado le como bug de fisica | `fase1_enemies/*/screenshot.png` (flutuando) vs `fase1_settled` |
| 2026-07-30 | `local_note` | medicao de camera por forense de screenshot | Tentei medir o deslocamento da camada 4 por 4 caminhos: correlacao cruzada (aliasou no padrao periodico), borda de vao com limiar de soma de canais (contagem de bordas divergiu), centroide rosa do Kirby com mascara larga (pegou 12568 px, incluindo faixas lavanda do ceu) e com mascara estreita (nao achou o Kirby numa das capturas). Nenhum deu numero confiavel | Screenshot e uma projecao com muitas fontes de ambiguidade: padrao periodico, cores parecidas entre elementos, e a curva de DAC do emulador. Forense de imagem e a ferramenta ERRADA para medir estado interno | Instrumentar o probe para exportar `cameraX` e amostras da tabela de HScroll no bloco VLAB. A medicao passa de forense para leitura direta e exata. **Parar de insistir em forense quando o dado existe na ROM** | tentativas na sessao 002 |
| 2026-07-30 | `local_note` | cor no screenshot vs cor no CRAM | As cores do screenshot NAO estao na grade RGB333: medi `(119,87,49)` onde a paleta tem `(109,73,36)` | BlastEm converte cor de 9 bits para RGB com uma curva de DAC mais realista que `n*255/7`. O screenshot nao e uma amostra fiel do CRAM | Legalidade RGB333 so pode ser verificada no CRAM via VLAB, nunca no PNG. O gate `cram_rgb333_legal` ja faz certo; NAO adicionar um gate equivalente sobre screenshot, ele daria falso negativo | medicao da linha de terreno, sessao 002 |
| 2026-07-30 | `local_note` | raster / gradiente de ceu | Gradiente de ceu por H-int escrevendo o indice 1 de PAL0 deixou a faixa das montanhas MAGENTA na tela | `VDP_setBackgroundColor(0)` aponta o backdrop para CRAM[0], que era a chave magenta do PNG. Onde ambos os planos eram transparentes aparecia a chave | Gradiente de ceu deve dirigir o BACKDROP (CRAM 0), nao um indice de tile. Beneficio extra: o ceu passa a custar 1 entrada de CRAM e ZERO tiles | `out/evidence/fase1_stage/*/screenshot.png` (antes) vs `fase1_v2` (depois) |
| 2026-07-30 | `local_note` | migracao SGDK 2.11 | `fix16ToInt(..)` faz o build FALHAR com `error: This method is deprecated`, nao warning | SGDK 2.11 marca os helpers fix16 antigos com `_Pragma("GCC error ...")` | Usar `F16_toInt(..)`. Vale a pena varrer a tabela de migracao do AGENTS.md por outros helpers com o mesmo tratamento antes de escrever codigo novo | log de build da sessao 002 |
| 2026-07-30 | `local_note` | medicao de parallax por screenshot | Correlacao cruzada da banda do terreno devolveu +42 px, inconsistente com a camera inferida das outras bandas (~118 px) | O terreno usa padrao xadrez periodico; correlacao cruzada em sinal periodico aliasa e trava num lag errado | Medir deslocamento por FEICAO UNICA (borda de vao, marco assimetrico), nao por correlacao, quando a textura for periodica. Ou instrumentar o probe para exportar a tabela de HScroll | `CHANGES.md` sessao 002 |
| 2026-07-30 | `local_note` | captura BlastEm em Linux | `linux_blastem_capture_status=blocked reason=window_timeout` e o processo `blastem.bin` ficou pendurado, travando a captura seguinte | Timeout da janela deixa o emulador orfao; a proxima captura disputa o X11 | Antes de reexecutar captura apos timeout, matar o `blastem.bin` orfao por PID. NAO usar `pkill -f blastem`: o padrao casa com o proprio comando do agente e mata o shell | sessao 002, exit 144 |
| 2026-07-29 | `local_note` | criterio de aceite de arte / luminancia | Pedi "luminancia media da camada 5 entre 0.24 e 0.27". O executor precisou adicionar 4454 px de micro-dither para caber na janela apos snap na grade RGB333, criando 8 cores novas e estourando o teto de 45 cores (foi para 47) | Janela apertada em ponto flutuante sobre espaco discreto de 8 niveis por canal e praticamente inalcancavel sem dithering; o criterio empurrou o executor para o defeito | Criterio de valor em espaco indexado deve ser em DEGRAUS DE PALETA, nao em float: "remapear para tons ja existentes, 2 degraus abaixo da camada vizinha, zero cores novas". Teto de cores e gate mais forte que alvo de luminancia | `doc/art/R2_VERDICT_AND_R3_PACK.md` secao 4; `data/source_art/r2/r2_mechanical_validation.json` |
| 2026-07-29 | `local_note` | criterio de aceite de arte / formula | Eu media luminancia com `0.299/0.587/0.114` e o executor com sRGB `0.2126/0.7152/0.0722`. Mesma imagem, dois numeros (0.2496 vs 0.269899). Passou por sorte, nao por acordo | Nao especifiquei a formula no criterio de aceite | Toda metrica numerica em pedido de arte precisa vir com a FORMULA. Canonizada no projeto: `L = (0.2126R + 0.7152G + 0.0722B)/255` | `doc/art/R2_VERDICT_AND_R3_PACK.md` secao 5.1 |
| 2026-07-29 | `success_pattern` | loop construtor/critico com agente externo | O executor declarou no proprio relatorio a metrica que o reprovava (`palette_colors: 47`) e o hack que usou para atingir o alvo (`palette_snap_micro_dither ... purpose: enter the requested luminance interval`), em vez de esconder | Exigir relatorio mecanico com numeros por criterio, e nao apenas "feito", torna o desvio visivel e barato de corrigir | Manter a exigencia de relatorio mensuravel por criterio em todo handoff de arte. Verificar de forma independente de qualquer modo — confiei no numero e medi de novo; foi o que revelou a divergencia de formula | `data/source_art/r2/r2_mechanical_validation.json` |
| 2026-07-29 | `local_note` | contrato de arte / paleta do protagonista | A rampa de rosa que EU especifiquei no pacote R1 (`255,182,182` / `255,146,146` / `219,109,146`) produziu um Kirby salmao, puxado para o laranja, nao o rosa chiclete das referencias | Escolhi os tres tons pela grade RGB333 sem checar o matiz contra a referencia; a rampa nao tem lean de magenta | Rampa corrigida para `255,219,255` / `255,182,219` / `255,146,182` / `219,73,146` / `146,36,109`. Ao especificar paleta de personagem, validar MATIZ contra referencia antes de travar, nao so legalidade de canal | `doc/art/R1_VERDICT_AND_R2_PACK.md` secao 5; medicao da distribuicao em `r1-01/concept.png` |
| 2026-07-29 | `local_note` | pipeline de imagegen externa | Quantizacao do agente externo desviou a chave de transparencia: R1-03 entregou fundo `(255,0,219)` 56.4% em vez de `(255,0,255)`, com tres magentas coexistindo; R1-04 ficou com franja `(219,0,219)` | O quantizador trata a chave como cor comum e a move para o vizinho mais proximo da paleta reduzida | Pedido de arte deve declarar a chave como cor RESERVADA, excluida da quantizacao, e o julgamento deve MEDIR a chave (nao olhar). Gate: zero pixel magenta fora de `(255,0,255)` | medicao em `data/source_art/r1/r1-0*/concept.png` |
| 2026-07-29 | `success_pattern` | julgamento de arte por medicao | Pedir autocritica honesta em `notes.md` fez o agente externo prever o proprio defeito antes do julgamento (colapso das bochechas a 28px; geometria nao modular no R1-06) | Criterio de aceite explicito + pedido de autocritica reduz alegacao de conformidade falsa | Manter "diga do que voce nao gostou" em todo pacote de pedido de arte | `r1-01/notes.md`, `r1-06/notes.md` |
| 2026-07-29 | `needs_human_review` | build em host Linux | `tools/sgdk_wrapper/build.sh` falha com `make.exe": .../makefile.gen: No such file or directory`; `new_project.sh` falha com `cp.exe: cannot stat` | `env.sh` prepende `$GDK/bin` ao PATH e esse dir tem symlinks `cp -> cp.exe`, `mkdir -> mkdir.exe`, `rm -> rm.exe`; coreutils POSIX fica sombreado por binario Windows sob Wine, que nao entende `/mnt/...` | Usar SEMPRE `build_sgdk_wine_bridge.sh`. Para `new_project.sh`, copiar para tmp e reinjetar `/usr/bin` no PATH depois do `source env.sh`. NAO editar o canonico sem aprovacao humana. | saida do build na sessao 001; `out/logs/linux_wine_build_report.json` |
| 2026-07-29 | `needs_human_review` | bootstrap de projeto novo | `doc/10-memory-bank.md` recem-criado afirmava ROM `22a80b7c...` de 262144 bytes, `build_v002`, 5 assets versionados e blockers `visual_direction_failed`, tudo datado de mai/jun 2026 — antes do projeto existir | `new_project.sh` copia o memory bank do template COM o conteudo do template dentro; so os placeholders `__PROJECT_NAME__` sao substituidos | Primeira sessao de qualquer projeto novo DEVE zerar `doc/10-memory-bank.md`. E a autoridade #1 da HIERARQUIA DE VERDADE; herdado sujo, ele faz a sessao 2 decidir sobre fatos inexistentes | `doc/10-memory-bank.md` antes/depois na sessao 001 |
| 2026-07-29 | `local_note` | orquestracao de subagentes | 3 subagentes Opus lancados em paralelo; TODOS morreram com `You've hit your session limit` sem nenhum concluir. Harness chegou a ~80%, os outros a ~0% | Fan-out paralelo de agentes Opus consome o orcamento da sessao mais rapido do que qualquer um deles termina | Serializar: 1 subagente por vez, ou escrever documentos localmente. Fan-out paralelo so para tarefas curtas e independentes | notificacoes de task da sessao 001; `CHANGES.md` |
| 2026-07-29 | `local_note` | custo de cena | `scene_demo` do template gasta 90 scanlines/frame na secao `scene`, so desenhando texto | `VDP_drawText` e caro; nao e representativo do custo de jogo real | Nao usar a cena de texto como baseline de performance. Medir de novo quando houver tilemap real | `out/evidence/probe_scene_demo/.../gate_report.json` |

## Regras

- Falha sem evidencia deve ser marcada como hipotese.
- Solucao nao comprovada nao vira recomendacao.
- Se a falha indicar risco canonico, classifique como `needs_human_review`.
