# Success Patterns

Registre aqui apenas padroes que funcionaram neste projeto com evidencia rastreavel.

| Data | Classificacao | Contexto | Padrao observado | Evidencia | Limite de uso |
|---|---|---|---|---|---|
| [DATA] | `local_note` | [cena/sistema] | [o que funcionou] | [build/log/screenshot/hash] | [onde nao aplicar] |
| 2026-08-06 | `local_note` | validador de asset com autoteste | `validate_assets.py` ganhou `--self-test`, que roda os mesmos checks contra os PLACEHOLDERS atuais em vez das entregas pendentes. Provou que os checks disparam de verdade — achou costura de ladrilhamento em 3 assets que eu nao sabia que existia, e mostrou a escada de valor com numeros reais (0.511 / 0.419 / 0.361 / 0.308) | `out/logs/asset_validation_report.json` | Autoteste contra dado conhecido so vale se o dado conhecido nao for trivialmente perfeito. Placeholder gerado por script quase passa em tudo, entao ele testa o CANO, nao o criterio |
| 2026-08-06 | `local_note` | pedir arte final com base em evidencia, nao esperanca | O pacote R1 PROIBIA papeis `animated_sprite_final`/`res_direct` porque IA nao acerta grade de 8px e paleta indexada. Inverti isso no P1 — mas com justificativa medida: as 11 entregas de R1/R2/R3 vieram com **0.00% de pixels ilegais** e o R3 reduziu paleta sem cor nova | `doc/art/PRODUCTION_ASSET_PACK.md` secao 0 | Nao inverter uma proibicao por conveniencia. So porque o executor ja provou a capacidade especifica que a proibicao protegia |
| 2026-08-06 | `local_note` | trigonometria em tempo real no 68000 | Substituir `F16_sin()` por uma tabela de 64 entradas `s8` pre-computada devolveu **40 pontos de CPU** (111% -> 71%) com imagem identica. No 68000 sem FPU, seno por frame e quase sempre tabela | `lake_fix` vs `lake_opt` | Tabela custa ROM. Para calculo raro (uma vez por cena) a funcao e mais barata em espaco e igual em tempo |
| 2026-08-06 | `local_note` | resolver contradicao de contrato medindo | `PALETTES.md` §6.1 limitava o H-int a 1 word de CRAM com teto `[NAO MEDIDO]`, e §6.3 descrevia 16. Em vez de escolher uma das duas por intuicao, tornei a contagem CONFIGURAVEL (`RASTER_setWaterCramWords`) para uma captura poder subir ate aparecer lixo. 4 words provados | `lake_opt` + dump de CRAM | So vale quando o custo de errar e visivel na captura. Para efeito que falha em silencio, medir de outro jeito |
| 2026-08-06 | `local_note` | gastar a alavanca de degradacao ANTES de quebrar | `sprites_per_scanline` mediu 19 de 20 com 6 inimigos + 8 tufos + 12 tiros nas mesmas linhas — um sprite do flicker. Em vez de esperar quebrar, gastei a alavanca documentada em ARCHITECTURE.md 5 (particulas -> primeiro plano -> projeteis): camada 5 de 8 para 6 tufos. Resultado 18/20 | `ability2` (19) vs `ability_final` (18) | So funciona porque a ordem de degradacao foi escrita ANTES do problema. Escada inventada na hora e racionalizacao |
| 2026-08-06 | `local_note` | gate que exige que a recompensa FUNCIONE | `ability_moveset_fires` reprova qualquer captura que CONCEDA uma copy ability sem que nenhum moveset dispare. Esse era exatamente o estado real do projeto por varias sessoes: o chapeu aparecia e nao fazia nada | `ability_final` PASS | Vale para qualquer recompensa de gameplay: se o jogo concede algo, o gate deve exigir prova de que aquilo faz efeito, nao so de que foi concedido |
| 2026-08-06 | `local_note` | aceitar a reprovacao de um gate alheio | O selador canonico rejeitou a tela de game over com `blank_or_low_information_capture`. Em vez de contestar o gate ou baixar o limiar, aceitei o diagnostico: para alvo AAA uma tela que e um vazio E um defeito. Refiz com o mundo visivel atras da mensagem e o gradiente mudando de humor. Passou, e a tela ficou melhor — nao so aprovada | `gameover1` rejeitado vs `gameover2` selado | Nao vale quando o gate mede a grandeza errada (ver `screenshot_color_count`). A pergunta e: o gate esta descrevendo um defeito real do produto? |
| 2026-08-06 | `local_note` | custo de colisao AABB | Adicionar colisao por contato (pontas de 4 galhos + 8 macas + zona de inalar contra Kirby) custou **12 pontos de CPU**: p99 78% -> 90%. Nao e desprezivel, e escala com o numero de pares testados | `boss_arena_final` (78%) vs `bosstest_final` (90%) | O custo aqui e de N pares por frame sem broad-phase. Antes de otimizar colisao em outro lugar, MEDIR: pode ser irrelevante numa cena com poucas entidades |
| 2026-08-06 | `local_note` | orcamento de sprite / tamanho de hardware | Um sprite de hardware do MD vai ate 4x4 tiles = 32x32 px. **Kirby de 32x32 custa UM sprite, nao quatro.** Medido: Kirby + 8 tufos + 4 inimigos = 13 entidades = 15 sprites de hardware. O orcamento de 80 e muito mais folgado do que a intuicao "1 sprite por 8x8" sugere | `fase1_enemies` 15/80; boss 39/80 com 28 segmentos + 8 macas + rosto + Kirby | O limite POR SCANLINE (20) continua contando sprites, nao tiles, mas um sprite largo ocupa muitos pixels da cota de 320 px/linha. Sprite grande economiza contagem e gasta largura |
| 2026-08-06 | `local_note` | otimizacao antes de degradacao | A arena do boss estourou o budget (cpu p99 96%, 19 de 32 frames). Em vez de gastar a alavanca 2 da escada de degradacao, procurei DESPERDICIO primeiro: a camera da arena e estatica e eu reconstruia a tabela de HScroll de 224 linhas x 2 planos todo frame com valores identicos. Pular o rebuild quando a camera nao move: **p99 96% -> 78%, frames estourados 19 -> 0, DMA 1792 -> 896 B**, com resultado renderizado byte-identico | `boss_arena1` vs `boss_arena5` | Otimizacao so vem antes de degradacao quando ela e provadamente sem perda. Se mudasse um pixel, seria degradacao disfarcada |
| 2026-08-06 | `local_note` | playtest scriptado | Input gravado **no lado da ROM** (tabela compilada) em vez de injetar teclas no emulador. Frame-exato, versionado, sem depender de foco de janela ou timing do X11. Cobertura de 11/11 estados, incluindo `swallow` e `ability`, com `enemies_alive` caindo de 4 para 2 como corroboracao independente | `out/evidence/playtest_run1/*/gate_report.json` | Nao substitui teste humano de game feel; prova que o estado foi ALCANCADO, nao que e divertido |
| 2026-08-06 | `local_note` | escada de degradacao documentada | O boss falhou `zero_over_budget_frames` (2 frames, cpu p99 87%). Apliquei a **alavanca 1 exatamente como o ARCHITECTURE.md §5.1 previa** (interpolar a cada 2 frames em vez de todo frame): cpu p99 87% -> 75%, frames acima do budget 2 -> 0, veredito FAIL -> PASS | `boss_run4` vs `boss_lever1` | So funciona porque a escada foi escrita ANTES de existir o problema. Escada inventada depois do fato vira racionalizacao |
| 2026-08-06 | `local_note` | gate anti-vacuidade | O gate de prioridade reportou corretamente "amostrou 0 entradas, isso nao prova nada" na cena do boss, em vez de dar PASS silencioso. O tronco esta nas colunas 16-23 e a amostragem so le a coluna 0 | `boss_lever1/*/gate_report.json` | Confirma que vale a pena gastar codigo para distinguir "0 de 0" de "0 de N" |
| 2026-07-30 | `local_note` | orcamento de sprite / camada de primeiro plano | Teste de estresse deliberado: subir a camada 5 de 4 para 24 tufos numa unica fileira horizontal. Resultado: `sprites_per_frame` 25/80 PASSOU mas `sprites_per_scanline` 24/20 **FALHOU**. Provou que para uma faixa horizontal de sprites o limite que morde e o POR SCANLINE, nao o por frame | `out/evidence/fase1_stress24/*/gate_report.json` | Nao generalizar: para sprites espalhados verticalmente o limite por frame volta a ser o dominante |
| 2026-07-30 | `local_note` | interpretacao de telemetria | Suspeitei que a secao `sprite` (104 scanlines para 5 sprites) fosse custo que explodiria no boss. Medi com 25 sprites: caiu para 53. Com 9: 43. **A hipotese estava errada** — a secao e pico ruidoso e nao escala com contagem de sprite. O que escala e `cpu_load_p99` (39% -> 60% -> 43%) | tres bundles: `fase1_parallax_w12`, `fase1_stress24`, `fase1_quota8` | Medir antes de otimizar. Um pico isolado de telemetria nao e um custo por unidade ate que se prove escala |
| 2026-07-29 | `local_note` | loop construtor/critico com agente externo de imagem | Julgar arte por MEDICAO em vez de inspecao visual. Script com PIL+numpy conferindo legalidade RGB333 pixel a pixel, contagem de cores, luminancia por camada, chave de transparencia e downscale real para 28px. Achou 2 defeitos que o olho nao pegaria (chave virada para `(255,0,219)` em 56% dos pixels; camada de primeiro plano mais clara que o terreno por 0.030) | `doc/art/R1_VERDICT_AND_R2_PACK.md`, `doc/art/R2_VERDICT_AND_R3_PACK.md` | Nao substitui julgamento estetico humano. Mede conformidade, nao beleza. Nao prova nada sobre a arte rodando em ROM |
| 2026-07-29 | `local_note` | pacote de pedido de arte | Declarar a grade de cor legal do hardware (8 valores por canal) e a escala real de destino (28px) DENTRO do pedido, antes de qualquer geracao. Resultado: 11 de 11 entregaveis vieram com 0.00% de pixels ilegais na primeira rodada | `data/source_art/r1/*/concept.png` medidos | Vale para pedido a agente externo capaz de quantizar. Nao vale como atalho para pular a traducao manual para grade nativa |
| 2026-07-29 | `local_note` | criterio de parada de loop iterativo | Encerrar o loop quando os defeitos passam a ser dos CRITERIOS do critico e nao do trabalho. Aconteceu por 3 rodadas seguidas (janela de luminancia em float sobre grade discreta, formula nao especificada, "area do corpo" ambigua, rampa de rosa errada no contrato) | `doc/art/R2_VERDICT_AND_R3_PACK.md` secao 8.2 | Nao usar como desculpa para parar cedo. So vale quando o executor entregou exatamente o pedido em todas as rodadas recentes e as falhas foram rastreadas ao criterio |
| 2026-07-29 | `local_note` | handoff com agente externo | Exigir relatorio mecanico com numero POR CRITERIO, nao "feito". O executor declarou espontaneamente a metrica que o reprovava (`palette_colors: 47` contra teto 45) e o hack usado para atingir o alvo (`micro_dither ... purpose: enter the requested luminance interval`) | `data/source_art/r2/r2_mechanical_validation.json` | Verificar de forma independente de qualquer modo: foi a re-medicao que revelou que as duas partes usavam formulas de luminancia diferentes |

## Regras

- Nao transforme sucesso local em regra global.
- Nao registre preferencia estetica como skill tecnica.
- Nao use este arquivo para alterar `.agent`, registry ou `lib_case`.
