# v10 runtime visual review — closeout

Date: 2026-09-04

## Truth state

```text
status=runtime_visual_review_candidate
visual_pass=false
final_acceptance=false
ready_for_aaa=false
human_gate_ready=false
res_promotion=false
claim_ceiling=technical_temporal_probe
production_route=assisted_native_translation
```

This is an isolated v10 review branch. v04–v09, historical `res/`, historical
runtime and historical ROM remain outside this package. The package is not a
native-authored animation candidate.

## Authority and lineage

- Identity authority: `data/source_art/r1/r1-01/concept.png`
- R1 SHA-256: `591d310623aaf37426af1cb846a715c1fd88e905163364d64565278ed31303cd`
- Run visual route: persisted producer guides, primary `im_lanczos3` route;
  source lineage is explicitly `assisted_native_translation` and
  `mechanical_affine_probe`, not `native_reauthored`.
- Fire visual source was copied into the project staging tree and is bound to
  SHA-256 `bb074ae95676fb8a2108d363c03466740539e66a3336dae1b2881a5ff86470ef`.
- `spr_ph_kirby` is a `procedural_composed_from_authored` visual-lab control;
  it is not a final production asset.

## What was proven

- The generated indexed PNGs have palette index 0 transparent, PLTE length 16,
  and pass the pixel-contract validator as technical candidates.
- The dedicated scene 11 compiled and ran in BlastEm with the review resource
  visible. Evidence bundle:
  `out/forward_test_v10_runtime_visual_review/blastem/blastem-linux-20260904T033031Z-2046670/`
- ROM SHA-256: `2f2a7a2ce7c51f35ca8ee8fdf1c9ac2c784cc86150f15440da09bb329e8dcfbd`.
- Evidence bundle is `sealed`, `semantic_capture_valid=true`, with screenshot,
  SRAM and VDP dump bound to that ROM. Runtime snapshot reports 60.3 fps,
  22% maximum CPU load, jitter 7, one active sprite, and zero over-budget
  frames. The snapshot does not prove sustained performance.

## What failed or remains unproven

- All four canonical strip validators return `rc=1`, `status=error` because
  the package deliberately carries diagnostic/procedural lineart and affine
  probe lineage. No native-lineart claim is made.
- Motion validators return `rc=1`: `mechanical_probe_cannot_prove_motion`,
  `native_lineart_lineage_missing`; run additionally has pivot/support drift,
  inhale has duplicate/too-few-distinct-frame findings, and jump/float has
  `noncanonical_motion_profile`.
- Candidate validator returns `rc=1`, `status=error`,
  `maximum_proven_claim=none`, `human_gate_ready=false`. It reports strip,
  motion, blind-recognition, fidelity, reuse and binding failures.
- Run integrity measured `min_height=17`, `max_height=27`, ratio `1.5882`
  against maximum `1.35`: scale inconsistency is a real visual blocker.
- The 1× composition is legible as a review control, but the action package is
  not visually equivalent to R1 and is not suitable for `res/` promotion.
- `run_golden_validate.ps1` returned `rc=0` with 8/8 generalist guards, but no
  golden project was found; this is not game acceptance evidence.
- `validate_measurement_tools.py` returned `rc=1` (`17/18` self-checks): an
  unrelated legacy `validate_native_sprite_production.py` lacks self-check and
  several materialized project tools are v1.1.0 while canonical is v1.2.0.

## Exact validation commands and results

The four contracts were regenerated by:

```text
python3 data/builders/write_v10_review_contracts.py .
```

For each of `idle`, `run`, `jump_float`, and `inhale`:

```text
python3 tools/sgdk_wrapper/.agent/scripts/validate_animation_strip_artifact.py --input out/forward_test_v10_runtime_visual_review/contracts/<action>_strip_contract.json --project-root . --output out/forward_test_v10_runtime_visual_review/reports/<action>_canonical_strip_report.json
python3 tools/sgdk_wrapper/.agent/scripts/validate_motion_semantics.py --contract out/forward_test_v10_runtime_visual_review/contracts/<action>_strip_contract.json --project-root . --output out/forward_test_v10_runtime_visual_review/reports/<action>_motion_report.json
```

Results: all strip invocations `rc=1/status=error`; all motion invocations
`rc=1/status=error`. Aggregate:

```text
python3 data/builders/write_v10_candidate_reports.py .
python3 tools/sgdk_wrapper/.agent/scripts/validate_animation_candidate.py --manifest out/forward_test_v10_runtime_visual_review/contracts/animation_candidate_manifest.json --project-root . --output out/forward_test_v10_runtime_visual_review/reports/animation_candidate_report.json
```

Result: `rc=1`, `status=error`, `maximum_proven_claim=none`.

Canonical self-checks for strip, motion and candidate each returned `rc=0`.
Pixel-contract checks for `review_kirby.png` and `review_fire_fx.png` returned
`rc=0`; provenance audit returned `rc=0`, `verdict=OK`.

## Evidence outputs

- [1× composition](../out/forward_test_v10_runtime_visual_review/reports/composition_320x224_1x.png)
- [BlastEm screenshot](../out/forward_test_v10_runtime_visual_review/blastem/blastem-linux-20260904T033031Z-2046670/screenshot.png)
- [runtime burst contact sheet](../out/forward_test_v10_runtime_visual_review/blastem/blastem-linux-20260904T033031Z-2046670/runtime_burst_contact_sheet.png)
- [runtime GIF](../out/forward_test_v10_runtime_visual_review/blastem/blastem-linux-20260904T033031Z-2046670/runtime_animation.gif)
- [sealed evidence manifest](../out/forward_test_v10_runtime_visual_review/blastem/blastem-linux-20260904T033031Z-2046670/evidence_manifest.json)
- [candidate report](../out/forward_test_v10_runtime_visual_review/reports/animation_candidate_report.json)

The action-specific contact sheets, GIFs, 1×/2×/3×/8× views, and light/dark/
chroma previews are under `out/forward_test_v10_runtime_visual_review/reports/`.

## Next causal gate

The next gate is an authorized visual producer supplying one genuinely native
32×32 key pose. Then author native 1 px binary lineart, materials and shading,
derive inbetweens from the keys, use only canonical motion profiles, and rerun
the canonical strip, motion and candidate entrypoints. Human review remains
closed until those technical and perceptual blockers are resolved.
