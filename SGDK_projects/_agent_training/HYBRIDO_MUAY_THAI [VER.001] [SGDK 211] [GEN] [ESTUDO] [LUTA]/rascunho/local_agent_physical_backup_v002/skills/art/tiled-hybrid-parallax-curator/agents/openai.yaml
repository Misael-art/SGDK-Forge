interface:
  display_name: "Tiled Hybrid Parallax Curator"
  short_description: "Curadoria Tiled hibrida com parallax e oclusao MD"
  brand_color: "#3F7E6E"
  default_prompt: "Use $tiled-hybrid-parallax-curator when a Mega Drive scene combines Tiled JSON, tilesets, parallax plates, occlusion art, flip flags or a hybrid panorama/tilemap reconstruction."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
