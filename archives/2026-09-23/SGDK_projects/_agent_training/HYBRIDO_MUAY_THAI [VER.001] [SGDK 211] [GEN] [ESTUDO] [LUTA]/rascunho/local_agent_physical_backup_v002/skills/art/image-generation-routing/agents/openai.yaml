interface:
  display_name: "Image Generation Routing"
  short_description: "Roteia geracao de imagem com seguranca"
  brand_color: "#4E7AC7"
  default_prompt: "Use $image-generation-routing antes de gerar imagem, declarar bloqueio visual ou escolher fallback local; registre canal, risco e proximo comando."

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
