interface:
  display_name: "Scene Direction Curator"
  short_description: "Define cenarios monumentais e tecnicas de cena"
  brand_color: "#2D7B8C"
  default_prompt: "Use $scene-direction-curator para decidir se a cena deve ser minimal, competente, monumental ou signature-only antes de traducao, budget e runtime."
policy:
  allow_implicit_invocation: false
dependencies:
  tools: []
