interface:
  display_name: "Sprite Animation"
  short_description: "Planeja sprites, volume, fisica e pixel-perfect"
  brand_color: "#7A5BC2"
  default_prompt: "Use $sprite-animation para planejar ou revisar uma sprite sheet Mega Drive com estados, key poses, strips, pivots, timing em VBlank, style_motion_reverse_engineering, turnaround_tracking_contract, motion_physics_contract, state_transition_motion_contract, pixel_perfect_animation_pass, reuso de tiles e legibilidade em 320x224."

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
