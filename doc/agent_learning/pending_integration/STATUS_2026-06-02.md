# PATCH STATUS — pending_integration (2026-06-02)

> Atualizacao apos Canonical Hardening Full Repair.

## patch_validate_resources_chain_integration_v1.md — APLICADO

O patch foi integrado em `tools/sgdk_wrapper/validate_resources.ps1` (bloco
`GAME_PRODUCTION_CHAIN_INTEGRATION`, ~90 linhas) entre a computacao de
`canonical_summary` e o `WriteAllText` final.

### Divergencias entre o patch original e a integracao real

1. **Variavel do report**: o patch original referencia `$validationReport`;
   a integracao real usa `$results` (nome canonico de `validate_resources.ps1`).
2. **resolvedProductStatus**: o patch original assume a existencia da
   variavel; a integracao real usa `$productTaxonomy.product_status` (ja
   computado em `validate_resources.ps1` linha ~3172).
3. **auditDir**: o patch original usa `Join-Path $auditDir 'audit_game_design_contracts_report.json'`;
   a integracao real usa `Join-Path (Split-Path $REPORT_FILE -Parent) 'audit_game_design_contracts_report.json'`
   para garantir o mesmo diretorio de `validation_report.json`.
4. **Saida do auditor**: alem de `status`, `blocking_statuses`, `catalog_checks`
   e `cross_references` (esperados pelo patch), a integracao real tambem
   consome `creative_blocking_statuses`, `technical_artifact_codes`,
   `technical_artifact_status`, `semantic_audit_status`,
   `semantic_audit_repeated_effect_learning_notes`, `technical_ready`,
   `creative_ready` e `ready_for_aaa` (saida do auditor v2.0.0).
5. **Merge com $results**: a integracao adiciona os codigos do auditor
   em `results.blocking_statuses` e em `results.status_panel.creative_blocking_statuses`
   (merge unico, preservando entradas preexistentes).
6. **Sinal blocked**: quando `status=blocked` no auditor, a integracao
   chama `Add-BlockingStatus` com codigo `audit_game_design_contracts_blocked`
   e incrementa `results.summary.errors` se necessario.

### Validacao

- `validate_resources.ps1` ainda compila (parse OK).
- `audit_game_design_contracts.ps1` agora roda com 3 buckets + ready flags.
- Smoke test E2E: ver `out/ci/validation_report_smoke.json` apos rodar
  `tools/sgdk_wrapper/ci/run_all_contract_gates.ps1 -Mode smoke`.

### Acoes humanas

- Nenhuma. Patch totalmente absorvido. Pode ser marcado como
  `status: applied` em qualquer ferramenta de gestao de patches.

## patch_SGDK_GLOBAL_chain_v1.md — NAO APLICADO

Worktree preexistente ainda dirty em `tools/sgdk_wrapper/.agent/rules/SGDK_GLOBAL.md`
(HEAD=3496 linhas; working=~5154 linhas). Patch preservado em
`doc/agent_learning/pending_integration/patch_SGDK_GLOBAL_chain_v1.md`
para aplicacao manual quando o worktree for limpo.

## patch_AI_MEMORY_BANK_chain_v1.md — NAO APLICADO

Mesma situacao. Patch preservado.

## template_minimum_product_patch.md — NAO TOCADO

Fora do escopo do Canonical Hardening Full Repair (canonical_hardening
trata framework, nao template).

## product_status_taxonomy_patch.md — NAO TOCADO

Idem.

---

## Estado do worktree (referencia rapida)

| Arquivo | HEAD | working | status |
|---------|------|---------|--------|
| `SGDK_GLOBAL.md` | 3496 | ~5154 | dirty preexistente |
| `doc/06_AI_MEMORY_BANK.md` | dirty | dirty | dirty preexistente |
| `validate_resources.ps1` | 5470 | 5560+ | dirty preexistente + chain integration |
| `audit_game_design_contracts.ps1` | 442 | 552+ | reescrito (v2.0.0) |
| `aaa_scene_v1.json` | base | +224 | dirty preexistente (COMPATIBLE) |
| 3 schemas | base | +allOf | endurecido |
| imagegen_tool.py | base | +exit | endurecido |
| imagegen_profiles.json | base | +paths | endurecido |
| models/manifest.json | base | +2 entries | endurecido |
| test_game_design_contract_gates.ps1 | 562 | 880+ | tests 12-18 adicionados |
| test_schema_contract_gates.py | n/a | 280+ | NOVO |
| canonical_hardening_full_repair_report.md | n/a | 270+ | NOVO |
