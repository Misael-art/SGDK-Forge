# Forward-test consolidated gate v02

Date: 2026-09-02. Previous v01 files remain historical and are superseded by `doc/forward_test_portfolio_matrix_v02.json`.

## Result

`KIRBY_FAN GAME GROK BUILD` reached a staging `native_animation_candidate` in `out/forward_test_v02/`. It contains visually inspected 32×32 native cells, separate `idle`, `run`, and `inhale` strips, animated previews, 1×/320×224 evidence, pivot/foot/continuity reports, P/4bpp pixel reports, tile/DMA/scanline budgets, source lineage and hashes.

The package is not a final sprite sheet and is not promoted to `res/`. No runtime, ROM, BlastEm, AAA, or human approval claim is made.

## Independent portfolio status

- BLUE: prior route evidence retained; v02 scale rule is 48×64 primary and 48×48 challenger.
- Celestial Chase: remains blocked because no local Lio identity authority was found in the examined source set.
- Kirby CLOUDE: v02 source audit and same-canvas route verified from its local model sheet; animation not claimed.
- Kirby GROK BUILD: native animation candidate delivered in staging.
- MARE: prior invalid pose remains negative history; canonical Taina authority and 56×80 scale remain the only allowed route.

## Tooling corrections

- `native_same_canvas` preserves a 32×32 canvas even when occupied bbox is smaller.
- Source audit binds identity authority, exact hash, visual source contract and derivation.
- Route shootout rejects targets outside the declared scale contract.
- Human reviewers require a linked decision record and hash; agent review is explicitly non-human.
- Non-uniform chroma matte changes representation to connected exact-chroma extraction.
- Strip integrity now distinguishes a dominant interior body color from a matte and supports explicitly declared fixed-cell boundary contact; default boundary behavior remains blocking.
