interface:
  display_name: "Camera System SGDK"
  short_description: "Camera SGDK com bounds, locks e parallax"
  default_prompt: "Use $camera-system-sgdk para projetar dead zone, look-ahead, bounds, room lock, shake e parallax com owner unico."

policy:
  allow_implicit_invocation: true
