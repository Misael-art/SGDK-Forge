interface:
  display_name: "Strategy Game Design"
  short_description: "Orquestra tower defense por opt-in explicito"
  default_prompt: "Use $strategy-game-design somente com opt-in para strategy_tower_defense e delegue systems, entidades, input, budget, TDD e runtime."

policy:
  allow_implicit_invocation: false
