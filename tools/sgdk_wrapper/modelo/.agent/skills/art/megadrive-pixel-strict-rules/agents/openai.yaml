interface:
  display_name: "Mega Drive Pixel Strict Rules"
  short_description: "Leis de paleta, grid, VDP e fake pixel art"
  brand_color: "#B84A3A"
  default_prompt: "Use $megadrive-pixel-strict-rules para validar este sprite, tileset ou background contra paleta, grid 8x8, index 0 transparente, fake_pixel_art_rejection e limites reais do VDP."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
