interface:
  display_name: "Sprite Animation"
  short_description: "Planeja e valida sprites, pivots e fluxo"
  brand_color: "#7A5BC2"
  default_prompt: "Use $sprite-animation para planejar ou revisar uma sprite sheet Mega Drive com roteiro de estados, key poses, strips, pivots, timing em VBlank, reuso de tiles e legibilidade em 320x224."

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
