# Workflow: AAA Scene Pipeline

Ordem canonica para uma cena visual de barra AAA.

Fonte machine-readable: `pipelines/aaa_scene_v1.json`.

Este arquivo nao substitui as skills. Ele so define sequencia, minima entrada, minima saida e passagem segura.

---

| # | Etapa | Entrada minima | Saida minima | Passa quando | Handoff |
|---|-------|----------------|--------------|--------------|---------|
| 0 | Escopo humano | classificacao previa via `workflows/project-opening.md`; quando a rota tecnica ainda nao estiver clara, abrir `workflows/route-decision-gate.md`; para projeto existente, `doc/11-gdd.md` e `doc/13-spec-cenas.md`; para projeto novo ou reseed, respostas do questionario de fundacao | briefing com objetivo, limites e criterio de aceitacao; em projeto novo ou reseed, usar `planning/game-design-planning` para emitir `project_brief`, `scene_roadmap`, `first_playable_slice`, `route_decision_record`, `roteiro_scope` e `front_end_profile`; quando houver HUD/UI formal, `ui_decision_card` declarado; quando houver peso tipografico real, `glyph_manifest` + anexo tipografico declarados; quando houver texto dramatico, fala, alerta cinetico ou flavor text, o anexo `ui_decision_card.text_presentation_profile` deve ser declarado dentro do mesmo card; quando houver transicao formal, `scene_transition_card` declarado; quando houver raster/luz/FX, boss/setpiece, tilemap avancado ou audio senior, declarar `feedback_fx_decision_card`, `boss_setpiece_card`, `advanced_tilemap_design_card` ou `audio_architecture_card`; em menu/title/front-end, usar `profile_kind=front_end_profile`; antes de gerar arte original, sourcing externo ou codigo SGDK sensivel, declarar `context_pack_manifest` | o contexto foi classificado corretamente; escopo fechado sem creep e, quando aplicavel, seeds de planejamento, rota tecnica, UI, texto, transicao, FX, boss, tilemap, audio e contexto consultado declarados antes do runtime | `art/art-direction-selector` |
| 0b | `art/art-direction-selector` | briefing aceito, `doc/11-gdd.md`, `doc/13-spec-cenas.md`, `doc/01_game_design/02_reference_pillars.md` quando existir e `tools/sgdk_wrapper/.agent/references/art_style_catalog.json` | `art_direction_decision_record`, `style_candidate_matrix`, `master_style_manifest`, `style_drift_policy` e `style_drift_correction_brief` quando houver drift | catalogo consultado; top candidatos avaliados; estilo escolhido tem compatibilidade Mega Drive, pressao VDP e risco de clone declarados; prompts futuros usam descritores tecnicos neutros, nao nomes de artista, estudio, marca, jogo ou IP como comando de copia | `art/art-asset-diagnostic` |
| 1 | `art/art-asset-diagnostic` | `res/`, `res/data/`, `.res` | laudo de cenario + issues bloqueantes | rota de arte decidida e registrada | `art/multi-plane-composition` ou `art/art-conversion-pipeline` |
| 1a | `art/art-creation-sourcing` quando `asset_route_decision=3_no_art` | laudo de diagnostico, briefing, GDD/spec, `art_direction_decision_record`, `master_style_manifest`, feedback bank, memoria global e memoria do projeto quando existir | `context_pack_manifest`, `tooling_capability_report`, `generation_channel_decision`, `art_generation_brief`, `asset_lineage_record` quando houver resultado bruto, `premium_source_manifest` quando houver arte aceita e `style_memory_index` quando houver familia visual; para personagem animado, `asset_kind_declaration`, `animation_state_plan`, `pose_roster`, `frame_budget_table`, `pivot_and_scale_contract`, `motion_phase_map` e `frame_delta_report`; para lutador/golpe/dano/boss premium, `animation_direction_contract`, `timing_spacing_report`, `impact_frame_contract`, `recovery_curve_report`, `hit_reaction_contract`, `shading_motion_report`, `palette_flash_policy`, `idle_breathing_cycle_contract`, `facial_expression_phase_map`, `cloth_secondary_animation_contract`, `hand_pose_keyframe_contract` e contratos de smear/modular rig quando aplicavel; para assets autorais, `authorial_model_sheet`, `authorial_stage_concept`, `clone_risk_report`, `source_validity_report` e `white_material_palette_contract` quando aplicavel | nenhum prompt de imagem ou download ocorre sem contexto e direcao de arte declarados; bloqueio de imagem so ocorre depois de diferenciar callable nativo, geracao inline, API/CLI e procedural; imagem inline fica `generated_inline_pending_persistence`, nao blocked; arte premium aceita precisa existir em `data/source_art/`; benchmark tecnico nao vira `source_art`; `local_author_pixel_rasterization` e `procedural_renderer` ficam restritos a `debug_lab`; personagem animado nao gera imagem antes do roteiro de estados, poses, frames, pivots e, quando premium, direcao de movimento; `key_pose_sheet` nao vale como `animation_strip`; cada strip tem uma unica acao e delta entre frames; golpe sem anticipation/active/recovery/hitstop, smear sujo ou dano sem direcao de forca volta para rework; assets incoerentes entram em `qa_correction_loop`; banco vetorial nao e dependencia v1 | `S1b_authoriality_gate` |
| 1b | `authoriality_gate` | `context_pack_manifest`, `master_style_manifest`, `premium_source_manifest`, model sheet autoral, conceito autoral de cenario e benchmark declarado | `source_validity_report`, `authoriality_gate_report`, `clone_risk_report`; `white_material_palette_contract` quando houver gi branco ou tecido claro | `source_validity=true` antes de qualquer `source_to_rom_visual_match`; personagem principal tem `authorial_model_sheet`; cenario tem `authorial_stage_concept`; manifesto declara `license`, `authorial_source`, `derivative_of`, `derivative_license_status`, `clone_risk_score`, `clone_risk_method` e `benchmark_used_as`; `clone_risk_score` e `benchmark_similarity_index` respeitam os limites declarados; benchmark orienta escala, densidade, timing, presenca, budget e qualidade, nunca fonte visual | `art/multi-plane-composition` |
| 2 | `art/multi-plane-composition` | mapa de composicao, spec da cena, referencia visual e `art_direction_decision_record` quando existir | `depth_role_map`, `composition_schema`, `layer_plan`, `shared_canvas_contract`, `scene_direction_seed`, `hardware_budget_review`, `delivery_findings` e cards formais quando houver UI/transicao/FX/boss/tilemap | planos, parallax e estrategia de ROM declarados; se houver rotas, a mesma cena continua travada por um `shared_canvas_contract` unico; cards formais saem com ownership, arquitetura, fallback, leitura e budget preliminar congelados; cena com ambicao de profundidade/clima/setpiece e encaminhada para direcao de cenario | `art/scene-direction-curator` |
| 2b | `art/scene-direction-curator` | `composition_schema`, `depth_role_map`, `layer_plan`, `shared_canvas_contract`, `art_direction_decision_record`, GDD/spec e `references/scene_archetype_catalog.json` | `scene_direction_record`, `scene_signature_techniques`, `fallback_profile`; quando aplicavel, `parallax_layer_contract`, `palette_cycle_decision_card`, `raster_fx_ownership_map` e `background_ecology_card` | catalogo consultado; perfil `minimal`, `competent`, `monumental` ou `signature_only` declarado; tecnicas assinadas possuem funcao de gameplay/narrativa, owner, fallback e budget preliminar; Mode 7 foi redirecionado para tecnica real do Mega Drive | `art/art-translation-to-vdp` |
| 3 | `art/art-translation-to-vdp` | `source_image`, alvo, `layer_plan`, `scene_direction_record`, cards de cenario quando existirem, referencia e expectativas de hardware; `source_validity_report` e `authoriality_gate_report` quando houver asset critico | `semantic_parse_report`, `translation_report`, `basic`, `elite`, `review estrutural` + opcionalmente `route_exploration_board`, `route_comparison_matrix`, `route_decision_record` | `source_validity` passou antes de traduzir ou medir match; a traducao preserva leitura e a direcao de cenario, gera artefatos auditaveis e congela uma direcao se houver multiplas rotas fortes | `art/visual-excellence-standards` |
| 4 | `art/visual-excellence-standards` | assets por layer, benchmark, `scene_direction_record` e cards de cenario quando existirem, cards formais quando existirem, `master_style_manifest`/`asset_lineage_record` quando houver arte gerada, `premium_source_manifest`, `source_validity_report`, `authoriality_gate_report`, `clone_risk_report`, contratos de animacao premium quando houver asset critico animado e `route_exploration_board` quando existir | parecer estetico + status por asset + `style_cohesion_check` + `scene_monumentality_check` + `motion_performance_check` + `visual_delivery_gate_report`; `source_to_rom_asset_map` e `benchmark_match_report` quando houver ROM/res ou benchmark; `white_material_palette_check`, `hud_formality_check`, `qa_findings` e `correction_request` quando houver drift | nenhum asset critico fica em `rework`, `needs_review`, `placeholder`, `debug_lab` ou `benchmark-derived`; `perceptual_quality` nao pode ficar `nao_medido`; cenario monumental sem funcao de gameplay/narrativa, budget ou card correspondente bloqueia; asset critico em `res/` exige `elite_ready` e `source_to_rom_visual_match >= 8`; lutador/golpe/dano/boss premium exige timing/spacing, hitstop frame, recovery, shading motion e contratos aplicaveis aprovados; quando houver `benchmark_profile.required_match`, `benchmark_match` precisa atingi-lo sem clonar; `PALETTE_WASTE`, contrato de gi branco ausente ou HUD com aparencia debug bloqueiam delivery; asset bonito mas fora do mundo visual e rejeitado | `hardware/megadrive-vdp-budget-analyst` |
| 5 | `hardware/megadrive-vdp-budget-analyst` | `.res`, dimensoes, `build_output.log` quando existir, parametros reais de runtime, `scene_direction_record`, cards de cenario, contratos de animacao premium e cards formais existentes | laudo `cabe` / `cabe com recuo` / `nao cabe`, com `fallback_reduced_scene_profile` quando necessario | budget explicito, motivado por numeros e coerente com UI, tipografia, texto expressivo, transicao, direcao de cenario, feedback FX, boss/setpiece, tilemap avancado, audio, ownership e fallback; pior frame ativo considera parallax, line scroll, CRAM cycle, H-Int slices, background actors, smear, spark, dust, flash/CRAM, hitstop e partes modulares de boss; `budget_pass` nao substitui `visual_pass` e folga de runtime nao justifica empobrecimento visual | `code/sgdk-runtime-coder` |
| 6 | `code/sgdk-runtime-coder` | specs, laudo de budget vigente, `.res`, `src/**/*.c`, `context_pack_manifest` quando houver API sensivel, `scene_direction_record`, cards de cenario, contratos de animacao premium e cards formais existentes | `runtime_decision_log`, `scene_direction_runtime_contract` quando aplicavel, `runtime_animation_timing_map`, `api_reality_check`, `build_evidence`, `delivery_findings` | build limpo, decisao de runtime coerente com budget; `api_reality_check` cita header SGDK ou fonte canonica local; startup, active, hitstop, recovery, FX/flash e retorno de estado seguem o contrato de animacao; cards formais registram owner, reset, fallback e handoff no `runtime_decision_log`; runtime de cenario monumental registra owners de scroll/CRAM/H-Int/tiles e fallback; runtime visual de producao nao inicia com `blocked_image_tooling`, `blocked_no_premium_source` ou `lab_not_delivery` | `validate_resources.ps1` |
| 7 | `validate_resources.ps1` + `freshness_audit.ps1` | projeto buildado, logs, `doc/changelog` | `validation_report.json`, `freshness_audit_report.json` quando houver evidencia previa | `summary.errors == 0`, drift observado e nenhum blocker de fechamento ativo | BlastEm + `build-validate.md` |
| 8 | BlastEm + `scene_closeout_gate.ps1` + `build-validate.md` | ROM, logs e captura | `emulator_session.json`, `scene_closeout_gate_report.json`, `freshness_audit_report.json`, evidencia dedicada, `doc/changelog`, `doc/10-memory-bank.md` atualizados | a ROM vigente foi observada com evidencia rastreavel e sem stale em artefatos obrigatorios | encerramento / proxima iteracao |

---

## Triagem arquitetural prioritaria

Antes da etapa 2, toda cena com perfil `aaa_layered` deve registrar uma triagem arquitetural minima:

- `scene_profile`
- `baseline_technique_applicability`
- `baseline_contract`
- `baseline_decision`
- `divergence_reason` quando houver divergencia
- `reference_implementation` quando houver referencia interna forte

Regra de decisao:

- use `tilemap streaming guiado pela camera` como baseline arquitetural prioritario para cenas com base + foreground + oclusao + relacao forte entre sprite e cenario
- trate essa prioridade como presuncao forte de metodo, nao como regra cega de replicacao
- se a tecnica for `sim` ou `parcial`, extraia explicitamente: divisao base/foreground, papel de cada plano, staging visual, organizacao de tilemaps, forma de oclusao e relacao sprite/cenario
- se houver uma referencia interna madura, como a `BLAZE_ENGINE [VER.001] [SGDK 211] [GEN] [ENGINE] [BRIGA DE RUA]`, registre-a como implementacao de apoio, nao como nome da tecnica
- se a decisao final for `divergir`, a justificativa deve citar constraints reais da cena antes de qualquer depuracao residual
- bugs de VRAM, paleta, `rescomp`, visibilidade de sprite ou workaround de `WINDOW` nao devem ser tratados como primeira linha quando a triagem arquitetural ainda nao foi fechada

---

## Dependencias Fixas

- Antes do primeiro build da sessao: `tools/sgdk_wrapper/preflight_host.ps1`
- Antes de gerar, buscar, converter ou julgar arte nova: `art_direction_decision_record` + `master_style_manifest` emitidos por `art/art-direction-selector`
- Antes de traduzir um cenario com profundidade, clima dinamico, pseudo-3D, bioma-chave ou setpiece de fundo: `scene_direction_record` emitido por `art/scene-direction-curator`
- Antes de declarar que a cena `cabe`: laudo da `megadrive-vdp-budget-analyst`
- Antes de declarar `validado` ou `AAA`: BlastEm + changelog + memoria operacional coerentes
- Antes de declarar `pronto`, `delivery` ou `AAA`: `visual_delivery_gate_report` sem blockers; `source_validity=false`, `authoriality_gate!=passed`, `clone_risk_score` ou `benchmark_similarity_index` acima dos limites declarados, `elite_ready=false`, `needs_review`, `placeholder`, `debug_lab`, `benchmark-derived`, `perceptual_quality=nao_medido`, `PALETTE_WASTE`, `source_to_rom_visual_match < 8`, `blocked_image_tooling`, `blocked_no_premium_source`, `lab_not_delivery`, `benchmark_match` abaixo de `benchmark_profile.required_match` quando aplicavel, `animation_direction_contract` ausente/falho para animacao critica, ou `local_author_pixel_rasterization` como fonte final bloqueiam a entrega
- Antes de encerrar cena: `tools/sgdk_wrapper/scene_closeout_gate.ps1` ou justificativa explicita do motivo de nao usar o orquestrador
- Depois de qualquer nova ROM/captura: `tools/sgdk_wrapper/freshness_audit.ps1`

---

## Escada Forense Obrigatoria

1. header PNG / PLTE
2. `rescomp` raw tiles
3. formula real de VRAM
4. decisao de arquitetura (`IMAGE`, `MAP`, streaming, `compare_flat`)
5. BlastEm
6. `freshness_audit.ps1`
7. `scene_closeout_gate_report.json` no fechamento

Se esta ordem nao foi cumprida, a analise nao esta completa.
